---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getImage } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import { getLang, useT, alternates } from '../i18n';

interface Props {
  title?: string;
  description?: string;
  /** Let the first section run under the transparent header (full-bleed heroes). */
  underHeader?: boolean;
  /** Share preview image (Signal, WhatsApp, social). Defaults to the title card. */
  image?: ImageMetadata;
  imageAlt?: string;
}
const lang = getLang(Astro);
const t = useT(lang);
const {
  title,
  description = t('site.description'),
  underHeader = false,
  image,
  imageAlt,
} = Astro.props;
const site = Astro.site ?? new URL('https://the-sound-of-entanglement.vercel.app');
const og = image
  ? new URL((await getImage({ src: image, width: 1200, height: 630, fit: 'cover', format: 'jpg', quality: 80 })).src, site).href
  : new URL('/og/default.jpg', site).href;
const ogAlt = image
  ? (imageAlt ?? '')
  : lang === 'de'
    ? 'BruQner im Mariendom Linz: projizierte Lichtwolken über dem leuchtenden Glaskasten mit dem Experiment.'
    : 'BruQner in the Mariendom Linz: clouds of projected light above the glowing glass case of the experiment.';
const alt = alternates(Astro.url.pathname);
const canonical = new URL(Astro.url.pathname, site).href;
const fullTitle = title ? `${title} · The Sound of Entanglement` : 'The Sound of Entanglement';
---
<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="alternate" hreflang="en" href={new URL(alt.en, site).href} />
    <link rel="alternate" hreflang="de" href={new URL(alt.de, site).href} />
    <link rel="alternate" hreflang="x-default" href={new URL(alt.en, site).href} />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content={lang === 'de' ? 'de_AT' : 'en_GB'} />
    <meta property="og:site_name" content="The Sound of Entanglement" />
    <meta property="og:url" content={canonical} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={og} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={ogAlt} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={og} />
    <meta name="theme-color" content="#000000" />
    {/* Preview phase: keep out of search engines until photo rights are cleared (docs/open-questions.md). Remove for launch, together with public/robots.txt. */}
    <meta name="robots" content="noindex, nofollow" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@300;400;600&family=Newsreader:ital,opsz,wght@0,6..72,300..600;1,6..72,300..600&display=swap"
    />
  </head>
  <body>
    <a class="skip ui" href="#main">{t('skip')}</a>
    <Header />
    <main id="main" class:list={{ 'under-header': underHeader }}>
      <slot />
    </main>
    <Footer />
    <style>
      main { padding-top: var(--header-h); }
      main.under-header { padding-top: 0; }
      .skip {
        position: absolute;
        left: 1rem;
        top: -3rem;
        z-index: 100;
        background: var(--paper);
        color: var(--black);
        padding: 0.5rem 1rem;
      }
      .skip:focus { top: 1rem; }
    </style>
  </body>
</html>
